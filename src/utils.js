// utils.js - Funciones de utilidad
// Usa este archivo para practicar merge y rebase

function sumar(a, b) {
  return a + b;
}

function restar(a, b) {
  return a - b;
}

// TODO: En los ejercicios añadirás más funciones aquí
function multiplicar(a, b) {
  return a * b;
}